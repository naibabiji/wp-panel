# 更新说明

## v1.2.32

### 优化：安全防御规则说明与「来源」列一一对应

- 「安全防御」页面右上角「防御规则说明」弹窗重新整理，封禁列表「来源」列的每个取值（Web防护、404防御、面板、扫描防御、SSH防护、手动封禁）现在都能在规则说明里找到对应的触发条件小节。
- 明确标注 Web防护 的 4 条触发规则（wp-login 暴力破解、xmlrpc、429、敏感文件扫描）共用同一个来源标签，无法仅从来源反查具体命中哪一条，需要结合「原因」列或站点日志判断。
- 补充此前未文档化的「面板登录防护」（面板登录/BasicAuth 连续认证失败封禁）触发条件说明。
- 补充敏感文件扫描（Web防护）与 404 泛滥检测（404防御）规则重叠说明：命中敏感文件的 404 请求会被两条规则同时计数，属预期设计，不是重复封禁。

仅涉及 `templates/firewall.html` 文案调整，无 API、字段或数据库变更。

## v1.2.31

### 修复：文件管理器解压中文 ZIP 文件名乱码

- 修复 Windows 中文环境创建的 ZIP 压缩包上传到服务器后，文件管理器解压出的中文文件名可能乱码的问题。
- ZIP 条目名会优先保留合法 UTF-8；遇到非 UTF-8 文件名时按 GB18030/GBK 解码，以兼容常见 Windows 中文压缩包。
- 解码后的文件名继续经过路径越权、特殊条目、文件锁和冲突检测，避免因为编码修复削弱解压安全检查。

### 验证

- 增加中文 ZIP 条目名、UTF-8 标志异常和解码后路径越权的单元测试。
