# 更新说明

## v1.2.30

### 改进：页面加载速度

- 优化设置页、告警设置、软件管理、网站详情等多内容页面的初始化流程，减少进入页面时等待所有接口返回的时间。
- 面板版本检查和系统更新检查仍会自动执行，但会延迟到页面主要内容加载后再运行，避免阻塞设置页展示。
- 多个可并行加载的数据会并行请求，非关键数据延后加载，降低页面首屏等待时间。

### 改进：安全防御页面

- 安全防御页面按需加载封禁历史、文件安全事件、WordPress 可疑访问分析等列表，减少进入页面时一次性加载大量记录造成的卡顿。
- 当前封禁列表新增筛选能力，可按 IP、封禁等级、来源快速定位记录。
- 文件安全事件和 WordPress 可疑访问记录保持独立查看，便于排查站点安全问题。

### 改进：防火墙封禁记录

- 同一个 IP 的活跃封禁记录会合并更新，避免永久封禁列表中重复出现大量相同 IP。
- 老版本遗留的重复活跃封禁记录会在升级后自动清理一次，无需手动处理。
- 永久封禁、临时封禁、手动封禁的来源和原因展示更一致，便于判断封禁来源。

### 数据库升级

- 新增一次性升级步骤，用于清理旧版本遗留的重复活跃防火墙封禁记录。升级过程不会删除历史记录，只会把重复的活跃记录标记为已解封，保留最有效的一条当前封禁记录。
