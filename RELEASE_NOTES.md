# 更新说明

## v1.3.1

### 新增

- **Swap 安全缓冲**：新安装或从旧版本首次升级时，为物理内存不超过 8GB、尚未启用 Swap 且磁盘空间安全的服务器自动创建 2GB Swap，并在仪表盘内存卡片显示使用情况。
- **OOM 事故告警**：自动识别系统内存耗尽时被内核终止的进程，在「告警管理 → 告警记录」中保留事故时间、进程和 PID；已配置邮件或 Webhook 时同步发送通知。
- **核心服务异常诊断**：记录 MariaDB、PHP-FPM、Nginx 和 Redis 的异常退出及自动恢复结果，并根据系统日志给出 OOM、进程崩溃、配置错误、端口占用、权限问题等初步原因。
- **事故资源快照**：在 OOM 或核心服务异常发生时，一次性记录内存、Swap、系统负载和高内存进程，帮助排查偶发故障，不增加日常持续采集负担。

### 优化

- 核心服务异常记录沿用现有告警入口和通知配置；面板内正常执行的启动、停止和重启不会作为异常事故告警。

## v1.3.0

### 新增

- **WordPress 单站点手动更新（核心 / 插件 / 主题）**：预览确认、下载与校验、更新前专用备份、维护模式、原子替换、健康检查、失败自动回滚；可对更新前创建的数据库备份手动恢复。
- **WordPress 库存采集与站点群总览**：只读采集组件、版本与可用更新候选，支持单站手动刷新与后台自动错峰刷新；新增独立 WP 总览页汇总所有站点状态。
- **插件批量更新**：单站点勾选多个插件一次性发起，串行执行；单项失败转人工处置（可单独回滚或忽略继续），不中断整批。
- **站点密码找回保护（三态）**：允许 / 全站禁用 / 仅禁用管理员，全站禁用同时隐藏登录页"忘记密码？"链接。
- **商业插件 / 主题授权更新**：支持已激活许可证的商业组件经站点原生更新机制更新（供应商 HTTPS 主机下载）；使用 `license://` 等非 HTTPS 自定义协议的主题/插件仍不支持，会明确提示走 WordPress 后台。
