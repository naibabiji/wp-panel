# 更新说明

## v1.2.32

### 优化：WordPress 可疑访问分析增加事件类型分类

- 「安全防御 → WordPress 可疑访问」现在会把每个 IP 的请求自动归类为以下四种类型之一，方便快速判断攻击意图：
  - **SQL 注入探测**：识别 `UNION SELECT`、`SLEEP()`、`information_schema` 等注入特征，`?s=color`、`?author=5` 等正常搜索请求不会误报。
  - **伪装搜索引擎爬虫**：UA 声称是 Googlebot/Bingbot 但来源 IP 不在官方 IP 段。
  - **敏感文件扫描**：`.env`、`.git`、`.sql`、`wp-config.php` 等敏感路径访问。
  - **不存在 PHP 文件探测**：扫描 `phpinfo.php`、`shell.php` 等不存在的 PHP 文件。
- 列表新增「事件类型」列和类型筛选下拉框，可以只看某一类可疑请求。
- 可疑访问记录改为持久化存储，日志轮转后历史数据不再丢失，保留最近 30 天。

### 新增：WordPress 安全探测告警

- 「告警通知」页面新增两条告警规则（默认关闭，需手动开启）：
  - **SQL 注入探测告警**：同一 IP 在统计窗口内触发 SQL 注入探测达到阈值时通知。
  - **伪装搜索引擎爬虫告警**：同一 IP 在统计窗口内触发伪装爬虫检测达到阈值时通知。
- 告警内容包含 IP、命中次数和热门路径样本，攻击持续期间每 24 小时重发一次避免刷屏。
- 告警阈值（默认 10 次）和统计窗口（默认 24 小时）可在告警页面直接调整，阈值范围 1-10000 次，窗口范围 1-168 小时。

### 新增：帮助反馈页面

- 侧边栏新增「帮助反馈」入口，集中展示 GitHub Issues、邮件和微信三种联系方式。
- 页面右侧介绍作者新开发的 Server Panel 项目——安全优先的自托管服务器管理面板，适合需要管理多台服务器和网站的用户，敏感凭据独立加密存储，查看需二次密码验证。

### 优化：安全防御规则说明与「来源」列一一对应

- 「安全防御」页面右上角「防御规则说明」弹窗重新整理，封禁列表「来源」列的每个取值（Web防护、404防御、面板、扫描防御、SSH防护、手动封禁）现在都能在规则说明里找到对应的触发条件小节。
- 明确标注 Web防护 的 4 条触发规则（wp-login 暴力破解、xmlrpc、429、敏感文件扫描）共用同一个来源标签，无法仅从来源反查具体命中哪一条，需要结合「原因」列或站点日志判断。
- 补充此前未文档化的「面板登录防护」（面板登录/BasicAuth 连续认证失败封禁）触发条件说明。
- 补充敏感文件扫描（Web防护）与 404 泛滥检测（404防御）规则重叠说明：命中敏感文件的 404 请求会被两条规则同时计数，属预期设计，不是重复封禁。
