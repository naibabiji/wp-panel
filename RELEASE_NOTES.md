# 更新说明

## v1.3.0

### 新增：WordPress 单站点手动更新（核心 / 插件 / 主题）

- 管理员可对单个 WordPress 站点手动检查并执行官方核心版本更新：预览确认、下载与签名/摘要校验、专用备份、维护模式、原子替换、健康检查、失败自动回滚。
- 官方仓库插件、主题支持同样流程的手动更新，复用同一套持久化任务与专用备份机制。
- 更新前的专用备份提供数据库 6 小时内复用、专用备份 24 小时保留的策略；复用旧数据库备份时，更新失败只自动恢复组件文件，不自动恢复数据库。
- 新增"更新备份"标签页，管理员可对更新前创建的数据库备份手动发起恢复；恢复前不会另行备份当前数据库。
- 手动数据库恢复与三种手动更新之间已加互斥锁，避免同一站点并发执行导致数据库并发写入损坏。

### 新增：WordPress 库存采集与站点群总览

- 新增只读组件库存采集（WordPress 版本、插件、主题、当前主题、可用更新候选），支持单站手动刷新和后台自动错峰刷新。
- 新增独立的 WP 总览页面，汇总所有 WordPress 站点的健康状态、版本、更新候选和最近扫描结果；总览与库存摘要对已满足的更新候选做隐藏，并支持过滤与分页。

### 新增：插件批量更新

- 支持对单个站点勾选多个需更新的插件一次性发起批量更新，由编排器串行派发（同一站点始终只有一个活跃任务，天然串行）。
- 批量内各项共享一次批量专用数据库备份；某一项失败后不自动回滚（`auto_rollback=0`），转为待人工处置，管理员可对每个失败项单独选择「回滚该项组件文件」或「忽略并继续」，无需整批中断。
- 编排器主循环以约 1 秒节奏驱动（某任务完成即触发下一项，30 秒 sweep 仅作兜底），并修复了重试退避比较格式 bug，进度页面新增「当前阶段」展示，整体派发速度接近 WordPress 后台。

### 新增：主题更新提示

- 当某站点仅有主题需要更新时，批量更新按钮给出明确提示：主题暂不支持批量更新，建议在 WordPress 后台或通过供应商渠道更新，或删除闲置主题后再试。

### 新增：站点密码找回保护（三态开关）

- 每个 WordPress 站点可在「允许（默认）/ 全站禁用 / 仅禁用管理员」三种模式间切换。
- 全站禁用会同时隐藏登录页"忘记密码？"链接；仅禁用管理员只限制管理员账户，其余用户不受影响。
- 通过面板托管的 mu-plugin 落地到 `wp-content/mu-plugins`，数据库仅记录当前模式；文件写入与状态落库在同一把站点锁内完成（apply 失败回滚、并发修改串行化），避免磁盘文件与数据库状态不一致。

### 新增：手动重新扫描主动触发更新检查

- 手动重新扫描站点组件信息时，会主动触发一次 WordPress 更新检查，使更新候选更快反映最新可用版本。

### 新增：更新日志标签页

- 新增「更新日志」标签页，支持一键复制与 24 小时自动清理。
- 更新日志中补充记录健康检查不通过与回滚失败的具体原因，便于排查。

### 新增：商业插件 / 主题授权更新支持（Plan B）

- 支持通过站点原生 WordPress 更新机制更新已激活许可证的商业插件/主题：当官方仓库无法提供时，允许从供应商自有 HTTPS 主机（路径可不含 `.zip` 后缀）按 vendor 外部 URL 策略下载安装包，并对下载地址做 SSRF 防护（DNS 重绑定 TOCTOU、运行时目录泄露）。
- 商业主题若使用非 HTTPS 的自定义授权下载协议（如 `license://`），面板暂不支持获取其更新包；现改为明确提示「该主题/插件使用商业授权下载方式，面板暂不支持，请通过 WordPress 后台更新」，不再误报为"状态已变化，请重新检查"。

### 优化与修正

- 完善 WP 更新失败原因分类与日志：暴露 upgrader `WP_Error` 明细、版本仍未更新等诊断信息，让"面板服务繁忙"等笼统报错暴露真实原因；并在更新日志中记录健康检查与回滚失败原因。
- 移除插件/主题更新健康检查中的逐文件 PHP 语法检查，缩短健康检查耗时。
- 收紧更新判定逻辑与过时文案；手动数据库恢复与手动更新之间互斥锁进一步加固，关闭 update-vs-restore TOCTOU 竞态。
- 核心更新面板在裸页面加载时不再错误"复活"已完成的任务；成功/失败横幅改为首次查看后自动消失。
- 修复批量更新数据库迁移顺序 bug（老装安装无法启动）、插件更新确认态、核心更新 canary、installer 修复依赖缺口、带既有站点的服务器上总览加载失败等问题。
- 英文翻译补全：消除 371 处 `EN_TODO` 占位符，en-US 与 zh-CN 文案对齐。
- 工作原理与限制改为页面右上角按钮弹窗，并补充文件锁定与更新流程兼容说明；最新版改为纯文字样式。
- 后台自动刷新 WordPress 库存采集、站点群总览页面过滤/分页、WP 可疑访问分析分类（含 URL 编码敏感路径）等多项细节优化。

### 尚未包含

- 定时后台检查 WordPress.org 新版本、后台自动下载缓存安装包、无人值守自动更新、多站点（multisite）批量更新——本次仍为管理员手动触发的单站更新，以及单站点的插件批量更新（主题批量更新暂不支持）。
- `interrupted_unknown`（更新任务中断且结果未知）状态的人工处置 UI/API，见下方"已知限制"手动处置步骤。

### 已知限制

- 如果某次更新任务因面板重启等原因进入"中断未知状态"（`interrupted_unknown`）且从未被确认，该站点会被后续所有更新请求持续拦截，当前版本没有 UI/API 处置入口。管理员需要先自行核实站点真实状态（组件文件和数据库是否已经完成更新），再手动执行以下 SQL 之一解除拦截：

  ```sql
  -- 核实后确认目标版本已经生效（文件和数据库都已是更新后的状态）
  UPDATE wp_update_tasks SET manual_disposition='confirmed_target_version', requires_attention=0
    WHERE id='<task_id>' AND task_kind='update' AND status='interrupted_unknown' AND manual_disposition='';

  -- 核实后确认更新未生效或状态不明，仅解除拦截、不做处理，后续需要人工关注
  UPDATE wp_update_tasks SET manual_disposition='marked_failed_no_action', requires_attention=1
    WHERE id='<task_id>' AND task_kind='update' AND status='interrupted_unknown' AND manual_disposition='';
  ```

- 面板自身重启会清空内存中的更新/恢复并发互斥锁；如果重启时恰好有更新或恢复任务在执行，需要人工核实其真实结果。
- 数据库遇到无法识别的历史版本号时会拒绝启动而不是带病继续运行，这是刻意的保护行为，不是缺陷。
- 更新专用备份的 24 小时自动清理和一次尚未开始执行的手动恢复请求之间存在极窄的时间窗口；如果窗口内备份被清理，恢复请求会安全失败并提示文件不可用，不会误操作或损坏数据。
- 使用 `license://` 等非 HTTPS 自定义授权下载协议的商业主题/插件，无法通过面板更新，请走 WordPress 后台或供应商渠道。

## v1.2.35

### 优化：安全防御记录管理

- 历史封禁记录展示范围由最近 30 条扩大到最近 300 条，并新增独立的 IP 搜索和分页功能。
- 当前封禁列表和 WordPress 可疑访问分析新增 IP 查询入口，可直接跳转 AbuseIPDB 查看网络归属和历史风险信息。
- 优化「填入封禁」操作，点击后自动切换到当前封禁列表、填入 IP 并定位到手动封禁输入框。

### 优化：安全记录页面布局

- WordPress 可疑访问分析的超长请求路径改为省略显示，完整内容仍可通过悬停和复制报告查看。
- 可疑访问分析的操作列固定在表格右侧，避免复制报告和填入封禁按钮被横向滚动隐藏。
- 当前封禁和历史封禁的超长原因限制显示宽度，避免来源、封禁时间等后续字段被挤出可视区域。

### 修正：WordPress 可疑访问分类

- 修复 `%2eenv` 等经过 URL 编码的敏感文件路径无法识别事件类型的问题。
- 编码后的 `.env` 等敏感路径现在可正确归类为「敏感文件扫描」。
- 本次调整仅改进安全日志分类和证据展示，不改变现有 Fail2ban 自动封禁规则。
