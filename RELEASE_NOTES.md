# 更新说明

## v1.2.29

### 新增：备份总览页面

- 新增独立的「备份总览」页面，可以跨网站查看所有网站的数据库备份、文件备份列表，以及面板自身数据库备份。
- 每个网站的备份列表默认折叠展示，避免网站数量或备份数量较多时页面难以浏览；页面顶部提供「全部展开」「全部收起」。
- 每条备份记录展示文件名、大小、时间、类型（全量/增量、自动/手动）以及本地/远程同步状态，点击网站名旁的按钮可直接跳转到对应网站详情页。

### 改进：远程备份可靠性

- 网站文件备份（智能增量模式）在判断走增量前，会先确认远程服务器上是否已有完整的全量备份基线；如果远程缺少全量基线（例如更换了远程备份服务器、或远程数据被清理），会自动改为全量备份重建基线，避免远程只留增量数据、无法完整恢复的情况。
- 数据库备份和文件备份同步到远程后的成功/失败状态会记录保存，即使设置了「不保留本地文件」，仍然可以在备份总览页看到该备份是否已经同步到远程。
- 备份总览页新增「核对远程状态」按钮，用于确认升级前已经同步到远程、但面板尚未记录状态的历史备份；点击后会核对一次远程服务器，并更新对应备份的远程状态显示。

### 新增：删除网站前的备份提醒

- 删除网站前，如果该网站还有数据库备份、文件备份或已配置的备份计划任务，面板会弹出提醒：删除网站不会自动删除这些备份文件或计划任务，需要管理员自行前往「备份总览」和「计划任务」页面清理。
- 网站关联的文件备份计划任务会在删除网站时自动禁用（不会被删除），避免网站删除后任务继续执行失败；任务本身仍需管理员自行删除。

### 数据库升级

- 新增网站文件备份记录表，用于备份总览页面展示；老用户升级后会自动一次性回填已有的本地文件备份记录，无需手动操作，不影响现有备份文件。

### 测试

- 覆盖备份总览接口、远程完整性检测、历史备份远程状态核对、删除网站前备份提醒等场景的自动化测试。
- `scripts/verify.sh` 已通过，覆盖 Go 测试、Go vet、构建、diff 检查和配套插件 PHP 语法检查。